# herdr headless recipes

> Placeholder — filled in by Step 0.2 (the herdr headless spike).

This file will record, verbatim, the working commands for driving herdr
headlessly (server start, named session, workspace with `--cwd`, running a
long-lived TUI in a pane, liveness detection, attach/detach/kill/cleanup, and
`events.subscribe` for `pane.agent_status_changed`). Until the spike runs, the
`HerdrEngine` recipe is unknown and `auto` engine selection falls back to tmux
(D4). Do not copy any herdr source into this repo (D3, AGPL) — record commands
and observed JSON shapes only.
